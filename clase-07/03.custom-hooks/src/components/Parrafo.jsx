
const Parrafo = ({ data }) => {
 
  return (
    <p>{data}</p>
  )
}

export default Parrafo

/* const Parrafo = (props) => {
  console.log(props) // props = { data }
  const { data } = props

  return (
    <p>{data}</p>
  )
}

export default Parrafo */